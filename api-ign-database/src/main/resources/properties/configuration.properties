# controls
controls=${controls}
# services
services=${services}
# projection
projection=${idee.proj.default}
# idee url
idee.url=${idee.url}
# max image size
max.image.size=1048576
# Proxy host (if was needed)
proxy.host=${proxy.host}
# Proxy port (if was need)
proxy.port=${proxy.port}